{
  "name": "moltpot",
  "version": "1.0.0",
  "description": "Deterministic jackpot ERC20 experiment.",
  "scripts": {
    "deploy": "hardhat run scripts/deploy.js --network hardhat"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  },
  "devDependencies": {
    "hardhat": "^2.22.0"
  }
}
